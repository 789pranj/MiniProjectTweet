<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MiniProject</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="../public/stylesheets/style.css">
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">

        <!-- Navigation Buttons -->
        <div class="flex justify-between w-full mb-5">
            <a class="bg-amber-600 rounded-md px-4 py-2 text-sm mb-5 inline-block hover:bg-amber-500 transition duration-300 ease-in-out" href="/profile">Back</a>
            <a class="bg-red-500 rounded-md px-4 py-2 text-sm mb-5 inline-block hover:bg-red-600 transition duration-300 ease-in-out" href="/logout">Logout</a>
        </div>
        
        <!-- Page Heading -->
        <h5 class="mb-5 text-2xl font-semibold">Edit Your Post</h5>

        <!-- Form to Update Post -->
        <form action="/update/<%= post._id %>" method="post">
            <textarea placeholder="What's on your mind ?" class="p-4 w-full sm:w-2/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md focus:ring-2 focus:ring-blue-400 focus:border-blue-500 transition duration-300" name="content"><%= post.content %></textarea>
            <input class="px-4 py-2 bg-yellow-500 block rounded-md mt-4 w-40 text-sm text-black cursor-pointer hover:bg-yellow-400 transition duration-300" type="submit" value="Update Post">
        </form>
    
    </div>
</body>
</html>
